{
  "name": "playwright-poc-js",
  "scripts": {
    "test": "cucumber-js",
    "trace:show": "npx playwright show-trace traces/*.zip"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^9.0.0",
    "@cucumber/pretty-formatter": "^1.0.1",
    "@playwright/test": "^1.35.0"
  }
}
